# 네트워크 게임 프로젝트 개선 사항 (2025-11-26)

본 문서는 오늘 진행된 네트워크 게임 프로젝트의 주요 개선 사항 및 코드 변경 내역을 상세하게 정리한 것입니다.

## 1. 인코딩 및 주석 복구

**문제 상황:**
기존 소스 파일(`NetworkManager.cpp` 등)의 한글 주석이 인코딩 문제(EUC-KR vs UTF-8)로 인해 깨져 있어 가독성이 떨어지는 상태였습니다.

**해결 방안:**
*   깨진 주석을 올바른 한글 내용으로 복구하고, 모든 소스 파일을 **UTF-8 with BOM** 인코딩으로 재저장하여 Visual Studio 및 기타 편집기에서의 호환성을 확보했습니다.

**변경된 파일:**
*   `GeometryDash/DaeWonFrameWork/NetworkManager.cpp`
*   `GeometryDash/DaeWonFrameWork/NetworkHeader.h`

---

## 2. 클라이언트 패킷 수신 로직 개선 (`NetworkManager::updatePacket`)

**문제 상황:**
클라이언트에서 `processPacket` 함수가 호출되지 않는 문제가 발생했습니다. 이는 논블로킹 소켓 설정 후 데이터 수신 로직(`recv`)에서 반환값 처리 미흡 및 버퍼 상태 확인 부재가 원인이었습니다.

**해결 방안:**
*   `NetworkManager::updatePacket` 함수에 **수신 버퍼 여유 공간 확인** 안전장치를 추가하여 버퍼 오버플로우를 방지했습니다.
*   `recv` 함수 반환값에 따른 처리를 명확히 했습니다.
    *   `> 0`: 데이터 수신 성공 -> `_recvBufferSize` 증가 및 `processPacket` 호출.
    *   `== 0`: 서버 연결 종료 -> `shutdown` 호출.
    *   `SOCKET_ERROR` (`WSAEWOULDBLOCK`): 데이터 없음(정상 대기) -> 리턴.
    *   `SOCKET_ERROR` (그 외): 에러 발생 -> `err_display` 및 `shutdown`.

**주요 코드 (`NetworkManager.cpp`):**

```cpp
void NetworkManager::updatePacket()
{
    if (_clientSocket == INVALID_SOCKET) return;

    // 안전장치: 버퍼 여유 공간 확인
    if ((int)(BUFFER_SIZE - _recvBufferSize) <= 0)
    {
        printf("[오류] 수신 버퍼가 가득 찼습니다.\n");
        return;
    }

    // 1. 데이터 수신 시도
    int bytesRecv = recv(_clientSocket,
        _recvBuffer + _recvBufferSize,
        (int)(BUFFER_SIZE - _recvBufferSize),
        0);

    if (bytesRecv > 0)
    {
        _recvBufferSize += bytesRecv;
        processPacket(); // 패킷 처리 함수 호출
    }
    else if (bytesRecv == 0)
    {
        printf("서버와 연결이 종료되었습니다.\n");
        shutdown();
    }
    else // WSAEWOULDBLOCK 등 에러 처리
    {
        int error = WSAGetLastError();
        if (error != WSAEWOULDBLOCK)
        {
            err_display("recv()");
            shutdown();
        }
    }
    
    trySendBuffer();
}
```

---

## 3. 디버그 출력 강화 (`NetworkHeader.h`)

**문제 상황:**
`printf`로만 에러를 출력할 경우, 윈도우 애플리케이션(GUI) 환경이나 Visual Studio 출력 창에서는 로그 확인이 어려웠습니다.

**해결 방안:**
*   `err_display` 함수를 수정하여 `OutputDebugStringA`를 함께 호출하도록 했습니다. 이를 통해 Visual Studio의 **'출력(Output)'** 탭에서 실시간으로 디버그 로그를 확인할 수 있게 되었습니다.

**주요 코드 (`NetworkHeader.h`):**

```cpp
inline void err_display(const char* msg)
{
    // ... (FormatMessage 등 기존 로직) ...
    printf("[%s] %s\n", msg, (char*)lpMsgBuf); // 콘솔 출력

    char debugBuf[256];
    snprintf(debugBuf, sizeof(debugBuf), "[%s] %s\n", msg, (char*)lpMsgBuf);
    OutputDebugStringA(debugBuf); // VS 출력 창 출력

    LocalFree(lpMsgBuf);
}
```

---

## 4. 플레이어 ID 재사용 및 관리 시스템 구축 (서버)

**문제 상황:**
기존에는 `_playerCount` 변수만을 사용하여 클라이언트 접속 시 단순히 증가시키는 방식이었습니다. 이로 인해 중간 번호의 플레이어(예: ID 0번)가 퇴장하더라도 그 ID를 재사용하지 못하고 계속해서 ID가 증가하는 문제가 있었으며, 이는 최대 접속자 수(`MAX_PLAYERS`) 제한과 맞물려 접속 불가 상황을 야기할 수 있었습니다.

**해결 방안:**
*   **ID 풀(Pool) 도입:** `std::priority_queue`를 사용하여 사용 가능한 ID(`_freeIds`)를 관리합니다. 접속 시 큐에서 ID를 꺼내고, 퇴장 시 다시 큐에 반환합니다.
*   **플레이어 활성 플래그 도입:** `Player` 구조체에 `isActive` 필드를 추가하여 해당 슬롯이 실제로 사용 중인지 명확히 구분합니다.
*   **벡터 -> 배열 변경:** `Room` 클래스의 `Players` 컨테이너를 `std::vector`에서 고정 크기 `std::array`로 변경하여 인덱스와 ID를 1:1로 매핑하고 관리 복잡도를 낮췄습니다.

### 4.1. Player 구조체 수정 (`Player.h`)

```cpp
struct Player
{
    // ... 기존 필드 ...
    size_t id{};
    bool isActive = false; // 활성 상태 플래그 추가
};
```

### 4.2. Server 클래스 수정 (`Server.h`, `Server.cpp`)

*   **멤버 변수 추가:** `std::priority_queue<int, ...> _freeIds;`
*   **초기화 (`initialize`):** 0부터 `MAX_PLAYERS-1`까지의 모든 ID를 큐에 삽입.
*   **접속 처리 (`acceptLoop`):**
    *   `_freeIds`가 비어있으면 접속 대기.
    *   접속 시 `_freeIds.top()`으로 ID 획득 후 `pop()`.
    *   `_room.AddPlayer(clientID)` 호출 시 획득한 ID 전달.
*   **ID 반환 (`ReturnClientID`):** `_freeIds.push(clientID)`를 통해 ID 재사용 가능하게 함.

**주요 코드 (`Server.cpp` - acceptLoop):**

```cpp
// 사용 가능한 ID가 있는 경우에만 진행
if (_freeIds.empty()) { /* 대기 */ }

// ... accept 성공 후 ...

int clientID = _freeIds.top(); // 가장 작은 빈 ID 획득
_freeIds.pop();

// 세션 초기화 및 Room에 추가
_clients._sessions[clientID].init(clientID, clientsocket, ClientState::Connected);
_room.AddPlayer(clientID); 
```

### 4.3. Room 클래스 수정 (`Room.h`, `Room.cpp`)

*   **컨테이너 변경:** `std::vector<Player> Players` -> `std::array<Player, MAX_PLAYERS> Players`
*   **`AddPlayer(int id)`:**
    *   전달받은 `id` 인덱스의 `Player` 객체를 초기화하고 `isActive = true`로 설정.
*   **`RemovePlayer(int id)`:**
    *   해당 `id`의 `Player` 객체를 `isActive = false`로 설정 및 상태 초기화.
    *   **`Server::Instance()->ReturnClientID(id)` 호출**하여 ID 반환.
    *   다른 클라이언트들에게 `S2C_DespawnOtherPlayerPacket` 전송 (퇴장 알림).
*   **`BroadcastState`:**
    *   `isActive`가 `true`인 플레이어 정보만 유효하게 패킷에 담아 전송.

**주요 코드 (`Room.cpp` - RemovePlayer):**

```cpp
void Room::RemovePlayer(int id)
{
    std::lock_guard<std::mutex> lock(_playerMutex);
    if (id >= 0 && id < MAX_PLAYERS && Players[id].isActive)
    {
        Players[id].isActive = false; // 비활성화
        Players[id].player_state = common::packet::Player_State::PLAYER_STATE_END;
        
        // 퇴장 패킷 브로드캐스트
        common::packet::S2C_DespawnOtherPlayerPacket despawnPacket;
        // ... 패킷 설정 ...
        BroadcastPacket(...);

        // ID 반환
        Server::Instance()->ReturnClientID(id);
    }
}
```

### 4.4. Session 클래스 수정 (`Session.cpp`)

*   **`Disconnect`:** 세션 연결이 끊길 때 `Room::RemovePlayer`를 호출하고, 만약 Room 처리가 안 되었을 경우를 대비해 안전하게 ID를 반환하도록 처리했습니다.

```cpp
void Session::Disconnect()
{
    if (_currentRoom != nullptr)
    {
        _currentRoom->RemovePlayer(_id); // 여기서 ID 반환 및 퇴장 패킷 전송 수행됨
    }
    // ... 소켓 종료 등 ...
}
```

---

## 요약

오늘 작업을 통해 클라이언트의 네트워크 안정성을 확보(인코딩 수정, 수신 로직 개선)하고, 서버의 확장성과 안정성을 위한 핵심 기능인 **플레이어 ID 재사용 시스템**을 완벽하게 구축했습니다. 이제 플레이어가 빈번하게 접속/종료하더라도 ID 고갈 문제없이 서버가 안정적으로 운영될 수 있습니다.

```