만약 클라이언트에서 블로킹 소켓을 사용하면, recv() 함수를 호출하는 순간 게임 전체가 멈추게 됩니다.

🛑 블로킹 소켓의 문제점 (클라이언트)
"Geometry Dash" 같은 실시간 게임은 '게임 루프(Game Loop)'가 1초에 60번(60FPS) 이상 실행되어야 합니다. 이 루프는 매번 다음 3가지 일을 합니다.

입력 처리 (스페이스 바를 눌렀는지?)

상태 업데이트 (캐릭터 위치 계산)

렌더링 (화면에 그리기)

만약 updatePacket() 함수가 블로킹 recv()를 호출하면, 서버에서 데이터가 올 때까지 게임 루프 전체가 recv()에서 멈춰버립니다.

그 결과, 서버가 패킷을 보내지 않는 동안에는:

화면이 멈춥니다 (렌더링 X)

키가 먹통이 됩니다 (입력 처리 X)

캐릭터가 움직이지 않습니다 (업데이트 X)

⚖️ 서버와 클라이언트의 차이

서버: "클라이언트별 스레드" 모델을 씁니다.  한 스레드가 한 클라이언트의 recv()를 기다리며 멈춰(블로킹) 있어도, 다른 클라이언트 스레드나 메인 스레드는 잘 돌아갑니다.


클라이언트: 메인 스레드 하나가 게임 루프(렌더링, 입력)와 updatePacket()(네트워크)을 모두 처리해야 합니다. 그래서 updatePacket()이 잠시라도 멈추면 안 됩니다.

결론입니다: initialize_Client에서 ioctlsocket을 사용해 소켓을 논블로킹으로 설정한 것은 아주 잘하신 결정입니다.

그렇게 해야 updatePacket()이 recv()를 호출했을 때, 데이터가 없으면(WSAEWOULDBLOCK) 즉시 리턴하고 게임 루프의 다음 작업(렌더링, 입력 처리)을 수행할 수 있습니다.